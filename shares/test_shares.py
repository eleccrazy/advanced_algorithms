import pytest
from shares import (
    build_graph,
    find_wccs,
    detect_cycle,
    process_tree_component,
    process_cycle_component,
    find_max_shares,
    read_input_from_file,
    write_output_to_file
)
